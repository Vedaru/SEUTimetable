<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- use DayNight parent so system UI adapts automatically -->
    <style name="Theme.SEUTimetable" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <style name="Theme.TransparentDialog" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>